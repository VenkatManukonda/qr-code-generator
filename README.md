# QR Code Generator using Python

This application generates a QR code from a user-provided URL using Python.

## Requirements
- Python 3.x
- qrcode library

## How to Run
1. Install dependencies:
   pip install -r requirements.txt
2. Run the program:
   python qr_generator.py
3. Enter a URL when prompted.
